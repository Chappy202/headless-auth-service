meta {
  name: Update Role
  type: http
  seq: 4
}

put {
  url: {{url}}/{{version}}/roles/:roleId
  body: json
  auth: bearer
}

params:path {
  roleId: 5
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  { "name": "senior_manager" }
}
