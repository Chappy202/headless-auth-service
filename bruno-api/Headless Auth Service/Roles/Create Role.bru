meta {
  name: Create Role
  type: http
  seq: 1
}

post {
  url: {{url}}/{{version}}/roles
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "manager"
  }
}
