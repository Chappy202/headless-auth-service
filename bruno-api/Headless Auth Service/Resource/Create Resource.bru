meta {
  name: Create Resource
  type: http
  seq: 1
}

post {
  url: {{url}}/{{version}}/resources
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "users",
    "description": "User management resource"
  }
}
