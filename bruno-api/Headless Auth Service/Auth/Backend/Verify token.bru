meta {
  name: Verify token
  type: http
  seq: 1
}

post {
  url: {{url}}/auth/introspect
  body: json
  auth: bearer
}

headers {
  X-API-KEY: {{api_key}}
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "token": "{{access_token}}"
  }
}
