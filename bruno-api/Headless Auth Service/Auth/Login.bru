meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{url}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "user",
    "password": "password" /*,
    "mfaToken": "optional_mfa_token"*/
  }
}

tests {
  let data = res.getBody();
  bru.setVar("access_token", data.access_token);
  bru.setVar("refresh_token", data.refresh_token);
}
