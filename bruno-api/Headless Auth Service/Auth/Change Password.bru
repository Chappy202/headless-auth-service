meta {
  name: Change Password
  type: http
  seq: 6
}

post {
  url: {{url}}/auth/reset-password
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "newPassword": "Password02"
  }
}
