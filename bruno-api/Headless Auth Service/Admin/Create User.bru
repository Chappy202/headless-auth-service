meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: {{url}}/admin/users
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "username": "newuser",
    /*"email": "newuser@example.com",*/
    "password": "StrongPassword123"
  }
}
