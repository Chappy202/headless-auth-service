meta {
  name: Delete User
  type: http
  seq: 5
}

delete {
  url: {{url}}/{{version}}/admin/users/:userId
  body: none
  auth: bearer
}

params:path {
  userId: {{userId}}
}

auth:bearer {
  token: {{token}}
}
