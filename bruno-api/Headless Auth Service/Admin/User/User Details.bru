meta {
  name: User Details
  type: http
  seq: 3
}

get {
  url: {{url}}/{{version}}/admin/users/:userId
  body: none
  auth: bearer
}

params:path {
  userId: {{userId}}
}

auth:bearer {
  token: {{token}}
}
