meta {
  name: Delete Role
  type: http
  seq: 5
}

delete {
  url: {{url}}/{{version}}/admin/roles/:roleId
  body: none
  auth: bearer
}

params:path {
  roleId: 7
}

auth:bearer {
  token: {{token}}
}
