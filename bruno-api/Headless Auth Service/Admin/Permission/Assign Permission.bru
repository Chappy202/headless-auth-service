meta {
  name: Assign Permission
  type: http
  seq: 1
}

post {
  url: {{url}}/{{version}}/admin/users/:userId/permissions/:permissionId
  body: none
  auth: bearer
}

params:path {
  permissionId: {{permissionId}}
  userId: {{userId}}
}

auth:bearer {
  token: {{token}}
}
