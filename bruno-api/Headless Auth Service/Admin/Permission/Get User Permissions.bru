meta {
  name: Get User Permissions
  type: http
  seq: 2
}

get {
  url: {{url}}/{{version}}/admin/users/:userId/permissions
  body: none
  auth: bearer
}

params:path {
  userId: {{userId}}
}

auth:bearer {
  token: {{token}}
}
