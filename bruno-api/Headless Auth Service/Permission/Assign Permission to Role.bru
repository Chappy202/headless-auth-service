meta {
  name: Assign Permission to Role
  type: http
  seq: 4
}

post {
  url: {{url}}/{{version}}/permissions/assign-to-role/:permissionId/:roleId
  body: none
  auth: bearer
}

params:path {
  roleId: 1
  permissionId: {{permissionId}}
}

auth:bearer {
  token: {{token}}
}
